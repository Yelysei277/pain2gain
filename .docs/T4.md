# T4 — Feed Page (Frontend)

**Goal:**  
Display generated ideas with filters in a simple, clean UI.

---

## 🍀 Deliverables

- Page `/feed`
- Fetches data from `/api/ideas`
- Filter by topic

---

## 🛠 Steps

1. Create page `src/app/feed/page.tsx`.
2. Fetch ideas:
   ```ts
   const ideas: ProductIdea[] = await fetch('/api/ideas').then(r => r.json());
   ```
3. Render list:
   - Title
   - Elevator Pitch
   - Pain Point
   - Topic (with color label)
   - Score
   - Source link
4. Add dropdown for topic filter (`all`, `devtools`, `health`, `education`, `other`).
5. Style:
   - Font: **Inter**
   - Background: light gray (`#F9FAFB`)
   - Cards: white with subtle shadow and rounded corners
   - Accent colors:
     - Red `#EF4444` (Pain)
     - Green `#22C55E` (Gain)
6. Keep layout responsive and minimalistic.
