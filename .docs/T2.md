# T2 — Two-Step LLM Analysis & Idea Generation

**Goal:**  
Filter relevant Reddit posts and generate structured product ideas.

---

## 🍀 Deliverables

- Type `ProductIdea`
- Functions:
  - `filterRelevantPosts()`
  - `generateIdeasFromPosts()`
  - `saveIdeas()`
- Output stored in `data/ideas.json`

---

## 🛠 Steps

1. Create `src/types/ideas.ts`:
   ```ts
   export interface ProductIdea {
     title: string;
     elevatorPitch: string;
     painPoint: string;
     topic: 'devtools' | 'health' | 'education' | 'other';
     score: number;
     source: { subreddit: string; url?: string };
   }
   ```
2. Implement LLM client in `src/lib/llm-client.ts`:
   - Wrapper around OpenAI API.
   - Function `callLLM(prompt: string): Promise<any>`.
3. Implement `src/lib/idea-generator.ts`:
   - `filterRelevantPosts(posts: RedditPost[]): Promise<RedditPost[]>`
   - `generateIdeasFromPosts(posts: RedditPost[]): Promise<ProductIdea[]>`
   - `saveIdeas(ideas: ProductIdea[]): Promise<void>`
4. Save generated ideas into `data/ideas.json`.
5. Ensure error handling and proper typing.
