# T3 — Minimal API Layer

**Goal:**  
Expose endpoints for idea generation and retrieval.

---

## 🍀 Deliverables

- `POST /api/generate`
- `GET /api/ideas`
- (Optional) `GET /api/ideas/random`

---

## 🛠 Steps

1. Create file `src/app/api/generate/route.ts`:
   - Load Reddit posts.
   - Run `filterRelevantPosts()` and `generateIdeasFromPosts()`.
   - Save ideas.
   - Return JSON with stats (e.g., generated count).
2. Create file `src/app/api/ideas/route.ts`:
   - Read `data/ideas.json`.
   - Return list of ideas as JSON.
3. (Optional) Create `src/app/api/ideas/random/route.ts`:
   - Return N random ideas from `ideas.json`.

---

## ⚙️ Implementation Notes

- Use Next.js App Router API style:
  ```ts
  import { NextResponse } from 'next/server';
  export async function GET() { ... }
  ```
- Proper error responses:
  - 400 for bad input
  - 500 for internal errors
- Always return valid JSON and typed data.
