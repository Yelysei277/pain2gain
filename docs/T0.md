
# T0 — Project Bootstrap

**Goal:**  
Create a clean TypeScript + Next.js base project ready for development.

---

## 🍀 Deliverables
- Next.js (App Router) setup  
- ESLint/Prettier configuration  
- Folder structure  
- `.env` variable support  

---

## 🛠 Steps
1. Initialize project:  
   ```bash
   npx create-next-app@latest --ts
   ```
2. Remove boilerplate demo pages.  
3. Set up linters and formatters:  
   - ESLint + Prettier (basic rules).  
4. Create folder structure:  
   ```
   src/
     app/
       feed/
       api/
     lib/
     types/
   data/
   ```
5. Add `.env.local.example` with:  
   - `OPENAI_API_KEY`  
   - `SUPABASE_URL`  
   - `SUPABASE_ANON_KEY`  
   - `EMAIL_API_KEY`  
